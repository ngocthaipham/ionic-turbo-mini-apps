{
  "name": "mfe-packages",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish",
    "registry": "verdaccio --config verdaccio.config.yaml",
    "publish:local": "pnpm build && pnpm publish:packages",
    "publish:packages": "pnpm --filter @your-org/types publish --no-git-checks && pnpm --filter @your-org/ui publish --no-git-checks && pnpm --filter @your-org/mfe-home publish --no-git-checks && pnpm --filter @your-org/mfe-products publish --no-git-checks"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.2",
    "turbo": "^2.5.4",
    "verdaccio": "^6.1.18"
  },
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  }
}
