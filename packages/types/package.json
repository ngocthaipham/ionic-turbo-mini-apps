{
  "name": "@repo/types",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'types package — no build step'",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
  }
}
